language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jadestyl' ]; then cd .. && eval "mv $currentfolder generator-jadestyl" && cd generator-jadestyl; fi

